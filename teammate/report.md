# Flickr30k 雙塔基線與延遲交互（Late Interaction）優化評估報告

本報告紀錄 `Qwen3-VL-Embedding-2B` 模型在 Flickr30k 測試集（1,000 張圖像，5,000 句文字說明）上的雙塔基線（Baseline）評估結果，以及優化後的延遲交互（Late Interaction）設定與最終檢索性能。

---

## 一、實驗設定與評估資料

* **評估模型**：`Qwen/Qwen3-VL-Embedding-2B` (BF16, 參數約 2B)
* **硬體環境**：Nvidia RTX 5080 (16GB VRAM)
* **資料集**：Flickr30k (Karpathy 測試集，共 1,000 張影像，每張影像對應 5 句 Captions，共 5,000 條文本)
* **圖像處理**：**使用原始解析度**，不設定解析度上限與 Thumbnail 限制。

---

## 二、評估方法與設定

我們針對不同的檢索排序（Ranking）方式進行了深入研究，探討全局語義與細粒度局部對齊的互補性：

### 1. 雙塔基線（Baseline / Pure EOS）設定
* **特徵提取**：取每個序列最後一個非 padding 標記（EOS / Last non-padding token）的 Hidden State 作為整體的稠密向量（Dense Vector，維度為 2048），並進行 L2 標準化。
* **相似度計算**：使用標準餘弦相似度（即兩個 normalized 向量的內積）。
  $$\text{Sim}_{\text{EOS}}(T, I) = \mathbf{e}_{T} \cdot \mathbf{e}_{I}$$

### 2. 單純延遲交互（Pure MaxSim, $\alpha=1.0$）設定
* **特徵提取**：提取影像和文本序列中**所有有效（非 padding）**的 Token 嵌入（embedding），保留詞標記及圖像塊標記的細粒度表示。
* **評分方法**：採用標準 ColBERT 風格的 Late Interaction 相似度。對於查詢序列 $Q$ 和候選對象 $D$，對 $Q$ 中的每個 token，尋找 $D$ 中與其餘弦相似度最高的 token，並將這些最大相似度進行平均歸一化：
  $$\text{Sim}_{\text{MaxSim}}(Q, D) = \frac{1}{|Q|} \sum_{i \in Q} \max_{j \in D} \left( \mathbf{q}_i \cdot \mathbf{d}_j \right)$$
* **面臨挑戰**：
  * **模板污染與雜訊**：模型輸入包含 Chat Template（如 `<|im_start|>user\n...`），這些固定模板 token 的相似度會干擾真實語義的細粒度對齊。
  * **表徵不匹配**：`Qwen3-VL-Embedding-2B` 是以對比學習（Contrastive Learning）為核心訓練的雙塔模型，其優化目標是使全局 EOS 向量對齊，而非 token-level 的 MaxSim。因此，直接使用純 MaxSim 會因缺乏 token 層級的對齊約束而導致分數失真。
  * **長度偏差（Length Bias）**：在圖像檢索文本（I2T）中，長文本因為包含更多 token，容易在 MaxSim 的最大值檢索中取得優勢，導致嚴重的排序崩潰（Recall 顯著下降）。

### 3. 我們改進的混合延遲交互（Hybrid MaxSim, $0.0 < \alpha < 1.0$）設定
為了解決純 MaxSim 的上述缺陷，我們設計了**混合延遲交互評分機制**。將全局語義（EOS）作為穩定錨點，並融入細粒度局部特徵（MaxSim），進行線性融合：
$$\text{Score}(A, B) = (1 - \alpha) \cdot \text{Sim}_{\text{EOS}}(A, B) + \alpha \cdot \text{Sim}_{\text{MaxSim}}(A, B)$$
* $\text{Sim}_{\text{EOS}}(A, B)$：全局 EOS 向量的相似度（捕捉宏觀語義）。
* $\text{Sim}_{\text{MaxSim}}(A, B)$：長度標準化後的 token 級最大相似度（捕捉細節對齊）。
* $\alpha$ 為混合權重超參數。當 $\alpha=0.0$ 時退化為雙塔基線，當 $\alpha=1.0$ 時為純 MaxSim。

---

## 三、評估結果對比（$\alpha$ 參數掃描）

下表展示了從純雙塔基線 ($\alpha=0.0$) 到純延遲交互 ($\alpha=1.0$) 之間，不同混合權重在文字檢索圖像（T2I）以及圖像檢索文本（I2T）任務上的性能表現：

| 排序方法 / $\alpha$ 值 | T2I R@1 | T2I R@5 | T2I R@10 | T2I MRR | I2T R@1 | I2T R@5 | I2T R@10 | I2T MRR |
| :--- | :---: | :---: | :---: | :---: | :---: | :---: | :---: | :---: |
| **純基線 (EOS, $\alpha=0.0$)** | 81.86% | 95.78% | 97.78% | 88.11% | 94.00% | 99.40% | 99.90% | 96.37% |
| 混合比例 ($\alpha=0.2$) | 82.28% | 95.86% | 97.96% | 88.42% | 94.20% | 99.40% | 99.90% | 96.47% |
| 混合比例 ($\alpha=0.4$) | 82.94% | 96.18% | 97.96% | 88.88% | 94.50% | 99.60% | 100.00% | 96.67% |
| **混合推薦 ($\alpha=0.5$)** | **83.18%** | **96.44%** | **98.00%** | **89.04%** | **94.80%** | **99.70%** | **100.00%** | **96.82%** |
| 混合比例 ($\alpha=0.6$) | 83.68% | 96.48% | 98.14% | 89.41% | 94.10% | 99.60% | 100.00% | 96.35% |
| 混合比例 ($\alpha=0.8$) | **84.18%** | **96.68%** | **98.24%** | **89.75%** | 92.20% | 99.10% | 99.70% | 95.03% |
| **純延遲交互 ($\alpha=1.0$)** | 80.68% | 95.38% | 97.58% | 87.29% | 78.40% | 95.30% | 98.00% | 85.78% |

---

## 四、結論與洞察

1. **單純 MaxSim ($\alpha=1.0$) 的效能崩潰**：
   * 在 T2I 上，純 MaxSim 的 R@1 降至 **80.68%**（比基線低 1.18%）。
   * 在 I2T 上更為嚴重，R@1 暴跌至 **78.40%**（比基線低 15.60%），MRR 也從 96.37% 降至 85.78%。
   * 這證實了在未經過 Token-level 排序微調的 VLM 上，**直接套用純 MaxSim 會帶來嚴重的長度偏差與模板雜訊污染**，導致檢索效果顯著惡化。

2. **混合機制（Hybrid）的互補優勢**：
   * 當引入適當的 EOS 權重（如 $\alpha=0.5$ 或 $0.6$）時，全局語義向量有效校正了 MaxSim 的長度偏置與匹配失真。
   * **最佳設定**：$\alpha=0.5$ 在 T2I 和 I2T 兩個檢索方向上皆能取得相較於基線的全面提升（T2I R@1 **+1.32%**，I2T R@1 **+0.80%**），是性能最均衡穩定的甜點區（Sweet Spot）。
   * **偏向 T2I 優化**：若主要應用於文字檢索圖像（T2I），將 $\alpha$ 設為 0.8 可以取得最高的 R@1 (**84.18%**，提升 **+2.32%**），但此時 I2T 會略微下降（R@1 降至 92.20%）。

3. **外部 ColBERT 專用模型的對比**：我們嘗試評估 `VAGOsolutions/SauerkrautLM-ColQwen3-2b-v0.1`（一個以 ColBERT Late Interaction 方式針對文件檢索專門訓練的模型）在 Flickr30k 上的表現。結果顯示 R@1 ≈ 0.1%，接近隨機水準，確認了**領域遷移（Domain Transfer）的挑戰**：該模型專為 PDF、發票等文件頁面對應查詢問題的場景訓練，直接應用於自然影像-文字描述的跨模態檢索任務時效果極差。這也反向驗證了我們使用 `Qwen3-VL-Embedding-2B`（以通用對比學習訓練）作為基底的合理性。

---

## 五、進階排序變體實驗（切片與平滑 MaxSim）

為了進一步提升檢索性能並消除模板雜訊，我們設計並探索了以下進階排序變體：

### 1. 去模板標記切片（Template Slicing）
在模型推理中，Chat Template（如 `<|im_start|>system\n...`）被用來包裝輸入。這些固定的模板 Token 在所有序列中完全一致，會在 MaxSim 計算中引入大量背景雜訊。
我們透過分析 Tokenizer，精確定位並切除（Slice）了模板 Token，僅保留核心語義 Token：
* **文本端**：切除前 14 個 Token（System 提示詞與 User 模板）以及末尾 3 個 Token（結束標記），保留主體文字 `tokens[14:-3]`。
* **圖像端**：切除前 15 個 Token 以及末尾 8 個 Token，僅保留中間對應影像 Patch 的 `tokens[15:-8]`（即 `<|image_pad|>` 的特徵表徵）。

### 2. 平滑最大值相似度（Soft-MaxSim / LogSumExp）
傳統 MaxSim 僅考慮每個 Query Token 的單一最佳匹配 Candidate Token（Hard Max）。我們使用 LogSumExp（LSE）將其平滑化，允許單個 Token 與多個局部區域進行軟匹配：
$$\text{Sim}_{\text{Soft-MaxSim}}(Q, D) = \frac{1}{|Q|} \sum_{i \in Q} \frac{1}{\beta} \log \sum_{j \in D} e^{\beta (\mathbf{q}_i \cdot \mathbf{d}_j)}$$
我們測試了溫度參數 $\beta = 10.0$ 與 $\beta = 20.0$。

### 3. 進階實驗結果對比

| 排序方法 | T2I R@1 | T2I R@5 | T2I MRR | I2T R@1 | I2T R@5 | I2T MRR |
| :--- | :---: | :---: | :---: | :---: | :---: | :---: |
| **雙塔基線 (Pure EOS)** | 81.86% | 95.78% | 88.11% | 94.00% | 99.40% | 96.37% |
| **未切片混合延遲交互 (Hybrid, $\alpha=0.5$)** | 83.18% | 96.44% | 89.04% | **94.80%** | **99.70%** | **96.82%** |
| **去模板切片純延遲交互 (Sliced Pure MaxSim)** | 70.90% | 89.44% | 79.20% | 71.80% | 90.50% | 80.45% |
| **去模板切片混合延遲交互 (Sliced Hybrid, $\alpha=0.5$)** | **83.66%** | **96.40%** | **89.37%** | 94.30% | 99.40% | 96.47% |
| **去模板切片混合延遲交互 (Sliced Hybrid, $\alpha=0.8$)** | 80.90% | 94.96% | 87.13% | 89.80% | 98.80% | 93.56% |
| **去模板切片平滑延遲交互 (Soft-MaxSim Pure, $\beta=10$)** | 58.08% | 80.92% | 68.75% | 8.40% | 31.50% | 20.25% |
| **去模板切片平滑混合延遲交互 (Soft-MaxSim Hybrid, $\beta=10, \alpha=0.5$)** | 81.90% | 95.92% | 88.19% | 92.60% | 99.20% | 95.41% |
| **去模板切片平滑延遲交互 (Soft-MaxSim Pure, $\beta=20$)** | 70.24% | 89.04% | 78.65% | 58.90% | 81.70% | 69.31% |
| **去模板切片平滑混合延遲交互 (Soft-MaxSim Hybrid, $\beta=20, \alpha=0.5$)** | 82.98% | 96.22% | 88.92% | 94.40% | 99.50% | 96.52% |

### 4. 進階洞察
* **切片去模板 (Slicing) 帶來 T2I 新高**：當結合 EOS 進行混合時，**去模板切片混合設定 (Sliced Hybrid, $\alpha=0.5$)** 在文字檢索圖像（T2I）上取得了 **83.66%** 的 R@1 歷史新高，相較於雙塔基線提升了 **+1.80%**，相較於未切片混合提升了 **+0.48%**。這說明去除重複的系統提示詞和引導詞能顯著提升細粒度圖像特徵的檢索精度。然而，在 I2T 任務上，未切片混合的表現（94.80%）仍略高於切片混合（94.30%）。這可能是因為部分模板特徵與 EOS 向量有一定程度的協同關係，切除後影響了 I2T 的全局表徵穩定度。
* **平滑最大值 (Soft-MaxSim) 參數的極大影響**：當 $\beta=10.0$ 時，不論是 Pure 還是 Hybrid，表現都大幅落後於 Hard MaxSim，特別是 Pure $\beta=10.0$ 的 I2T R@1 僅剩 **8.40%**。當將平滑度收斂至 $\beta=20.0$ 時，性能顯著回升（Soft-MaxSim Hybrid 0.5 達到 T2I R@1 82.98% / I2T R@1 94.40%），非常接近 Hard MaxSim 的表現。這證實了**延遲交互高度依賴於「稀疏且強烈的局部 token 對齊」**。如果平滑度過大（$\beta$ 過小），會使相似度被大量不相關的背景 token 匹配稀釋，導致排序能力下降。因此，Hard MaxSim 仍是首選。

---

## 六、創新排序變體實驗（雙向、IDF 加權與 Top-K MaxSim）

為了進一步挑戰檢索極限，我們設計並探索了三種極具啟發性的排序演算法變體：

### 1. 雙向細粒度對齊（Bi-directional MaxSim）
標準的 T2I MaxSim 是單向的（文字標記對應圖像區塊的最大值平均）。若一個只包含「草地」的影像去匹配「一隻狗在草地玩耍」的文字，單向匹配會因「草地」完美對齊而給予高分（偽陽性高）。
雙向對齊要求雙向皆須匹配：
$$\text{Sim}_{\text{BiMaxSim}}(T, I) = \frac{1}{2} \left( \text{Sim}_{\text{MaxSim}}(T \to I) + \text{Sim}_{\text{MaxSim}}(I \to T) \right)$$
這能大幅抑制局部過度匹配，強制要求影像與文字在細粒度上互為對應。

### 2. 逆文件頻率加權對齊（IDF-Weighted MaxSim）
在自然語言中，常見詞（如 "a", "the", "in"）重要性低，而專有名詞或罕見詞（如 "dog", "playing"）攜帶關鍵語義。
我們在 Flickr30k 訓練集上預先建立詞庫，統計所有標記的 Document Frequency（DF），並計算平滑後的 IDF 權重 $w_i$：
$$\text{Sim}_{\text{IDF-MaxSim}}(T \to I) = \frac{\sum_{i \in T} w_i \max_{j \in I} (\mathbf{t}_i \cdot \mathbf{i}_j)}{\sum_{i \in T} w_i}$$
這使得 MaxSim 能聚焦於查詢句中的核心名詞與動詞匹配，減少停用詞匹配引入的背景雜訊。

### 3. 多標記對應（Top-K MaxSim）
標準 MaxSim 僅將查詢標記對應到候選對象中相似度最高的唯一標記（Top-1）。我們將其擴展為 Top-K 相似度的平均值（如 $K=2, 3$）。這可提升局部特徵匹配的容錯率與魯棒性，避免因單一雜訊標記的高相似度導致評分偏置。

---

### 4. 創新實驗結果對比

| 排序方法 / 設定 | T2I R@1 | T2I R@5 | T2I MRR | I2T R@1 | I2T R@5 | I2T MRR |
| :--- | :---: | :---: | :---: | :---: | :---: | :---: |
| **雙塔基線 (Pure EOS)** | 81.86% | 95.78% | 88.11% | 94.00% | 99.40% | 96.37% |
| **去模板切片混合 (Sliced Hybrid, $\alpha=0.5$) [基準]** | 83.66% | 96.40% | 89.37% | 94.30% | 99.40% | 96.47% |
| **雙向切片純對齊 (Bi-directional Sliced Pure)** | 72.34% | 91.70% | 80.76% | 86.00% | 98.30% | 91.25% |
| **雙向切片混合對齊 (Bi-directional Sliced Hybrid, 0.5)** | 82.84% | 96.24% | 88.79% | 93.80% | **99.50%** | 96.39% |
| **IDF 加權切片純對齊 (IDF-Weighted Sliced Pure)** | 71.56% | 89.60% | 79.67% | 71.80% | 90.50% | 80.45% |
| **IDF 加權切片混合對齊 (IDF-Weighted Sliced Hybrid, 0.5)** | **84.12%** | **96.40%** | **89.61%** | 94.30% | 99.40% | 96.47% |
| **Top-2 切片純對齊 (Top-2 Sliced Pure)** | 71.44% | 90.02% | 79.65% | 68.20% | 87.60% | 77.23% |
| **Top-2 切片混合對齊 (Top-2 Sliced Hybrid, 0.5)** | 83.86% | 96.38% | 89.43% | 94.50% | 99.40% | 96.57% |
| **Top-3 切片純對齊 (Top-3 Sliced Pure)** | 71.02% | 89.66% | 79.22% | 63.20% | 84.80% | 73.09% |
| **Top-3 切片混合對齊 (Top-3 Sliced Hybrid, 0.5)** | 83.52% | 96.32% | 89.25% | **94.60%** | 99.40% | **96.65%** |

---

### 5. 核心結論與洞察

1. **IDF 加權（IDF-Weighting）大幅突破 T2I 極限**：
   * **IDF 加權混合設定 (IDF-Weighted Sliced Hybrid, 0.5)** 在文字檢索圖像（T2I）上取得了 **84.12%** 的 R@1，這是全系列實驗（包含 balanced 混合）的最高成績，相比於雙塔基線提升了 **+2.26%**。這證明了**在 Late Interaction 中結合傳統資訊檢索（IR）的權重機制能大幅提升匹配準確度**，成功使模型忽略無關聯的連接詞，鎖定實體詞進行匹配。

2. **雙向對齊（Bi-directional）為純延遲交互帶來革命性修正**：
   * 在不結合任何 EOS 的純 MaxSim 下，標準單向 MaxSim 的 I2T R@1 僅為 71.80%。
   * 引入 **雙向對齊 (Bi-directional Sliced Pure)** 後，I2T R@1 大幅攀升至 **86.00%**（提升 **+14.20%**！）。
   * 這強烈印證了我們的理論：雙向約束能有效過濾掉「部分語義匹配造成的干擾」，使得純 Token-level 的 late interaction 檢索性能產生本質上的飛躍。

3. **Top-K 匹配在特定方向帶來穩定增益**：
   * **Top-2 混合設定** 全面優於 Top-1（T2I R@1 83.86% vs 83.66%，I2T R@1 94.50% vs 94.30%）。
   * **Top-3 混合設定** 在 I2T R@1 取得了 **94.60%** 的佳績。這表明當影像 Patch 被映射至多個語義重疊的詞彙（例如 "dog" 和 "puppy"）時，平均多個相似度匹配點比單一極大值點更加穩健，減少了特徵噪訊的影響。

---

## 七、微調訓練基礎設施（已建立，結果待完成）

我們已在工作區建立完整的微調訓練基礎設施，供後續在 RTX 5080 上自行完成訓練：

### 建立的腳本

| 腳本 | 功能 |
| :--- | :--- |
| [`train_late_interaction.py`](file:///c:/Users/bojyu/Downloads/teammate_package/train_late_interaction.py) | 完整訓練流程：凍結骨幹，訓練 `Linear(2048→128)` 投影層，以 InfoNCE + MaxSim 損失微調 |
| [`cache_train_embeddings.py`](file:///c:/Users/bojyu/Downloads/teammate_package/cache_train_embeddings.py) | 加速方案：預先計算並快取訓練集的 2048 維 hidden states，訓練時直接讀取，跳過 VLM 前向推論（每步從 ~88 秒縮短至 < 1 秒） |

### 訓練策略說明

* **方案 A（直接訓練）**：每次訓練步驟需完整推論 Qwen3-VL 骨幹（耗時 ~88 秒/步），約需 22 小時以上，超出 12 小時限制。
* **方案 B（Embedding 快取）**：先以 `cache_train_embeddings.py` 將全部訓練集的 2048 維表示存至磁碟（一次性約 2–3 小時），後續每步訓練只需讀取快取並計算投影層梯度（< 1 秒/步），可在 1 小時內完成訓練。

```bash
# Step 1: 快取訓練集 embedding（執行一次，約 2–3 小時）
python cache_train_embeddings.py

# Step 2: 快速訓練投影層（約 30–60 分鐘）
python train_late_interaction.py --use_cache embedding_cache
```
